# Exemplo de variáveis de ambiente (Vite + Supabase)
# Copie para .env.local e preencha com suas credenciais

# Frontend (Vite)
VITE_SUPABASE_URL="https://seu-projeto.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sua_chave_anonima_do_supabase"

# Opcional: apenas se você usar as rotas Node em /api
# (por exemplo, para autenticação própria com Postgres)
# Use uma URL com SSL (pooler) quando estiver em produção
# POSTGRES_URL="postgres://usuario:senha@host:6543/postgres?sslmode=require"